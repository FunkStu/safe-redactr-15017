import { PIIDetector } from '@/components/PIIDetector';

const Index = () => {
  return (
    <div className="min-h-screen bg-gradient-to-br from-background to-muted/20">
      <PIIDetector />
    </div>
  );
};

export default Index;
